<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="60dp"

        >
        <EditText
            android:id="@+id/edit"
            android:layout_width="0dp"
            android:layout_height="50dp"
            android:layout_weight="1"
            android:hint="请输入您要查询的单词"
            tools:ignore="Suspicious0dp" />

        <Button

            android:id="@+id/search"
            android:layout_width="wrap_content"
            android:layout_height="50dp"
            android:text="查询"
            />

    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:orientation="horizontal">
        <TextView
            android:id="@+id/english"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:textSize="30dp"
            android:textColor="@android:color/holo_red_light"/>
    </LinearLayout>
    <TextView

        android:id="@+id/chinese"
        android:layout_width="match_parent"
        android:layout_height="40dp"

        android:textSize="30dp" />

    <!--String inputText = editText.getText().toString();-->
    <!--List<Word> words = LitePal.where("english= ?", inputText).find(Word.class);-->
    <!--if (words.isEmpty()) {-->
    <!--english.setText("未找到您想要查询的单词，请检查后重试");-->
    <!--} else {-->

    <!--String result_chinese = null;-->

    <!--for (Word word : words) {-->
    <!--result_chinese = word.getChinese();-->

    <!--}-->
    <!--english.setText(inputText);-->
    <!--chinese.setText(result_chinese);-->


    <!--}-->


</LinearLayout>